{
  "routes": [
    {
      "route": "/Admin/*",
      "allowedRoles": ["anonymous"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/Admin/*", "*.{css,js,png,jpg,gif,ico}"]
  },
  "globalHeaders": {
    "cache-control": "must-revalidate, max-age=3600"
  }
}